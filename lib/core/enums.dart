enum DeviceScreenType { Mobile, Tablet, Desktop }
enum AppointmentStatus {
  OnHold,
  Upcoming,
  Canceled,
  Finished,
  Inprogress,
  Waiting,
  Confirmed
}
enum AttachmentImage { Before, After, Profile }
enum PaymentStatus { Paid, PartialyPaid, notPaid }
enum TransferName { Plasma, Transfer2, Empty }
enum RoleName { Receptionist, Doctor, Administrator }
